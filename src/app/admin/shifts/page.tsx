// TODO: List all shifts grouped by date with their time block and assigned volunteer.
//       Provide a form to create a new shift (pick date, timeBlock, user).
//       Allow editing and deleting shifts via PUT /api/shifts/[id] and DELETE /api/shifts/[id].

export default function ShiftsPage() {
  return (
    <div className="max-w-3xl mx-auto">
      <h1 className="text-2xl font-bold text-gray-900 mb-4">Shifts</h1>
      <p className="text-gray-500 text-sm">
        TODO: Display all shifts grouped by date. Include a form to create, edit,
        and delete shifts. Show the assigned volunteer and associated time block.
      </p>
    </div>
  );
}
